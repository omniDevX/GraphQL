type Book {
    title: String
    author: String
}


type Listing {
    id: ID!
    title: String!
    numOfBeds: Int
    costPerNight: Float
    closedForBookings: Boolean
}


type Query {
    qBooks: [Book]
    qListings: [Listing!]!
}
